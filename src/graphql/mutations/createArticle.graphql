mutation CreateArticle($author: String!, $title: String!, $markdown: String!, $description: String!) {
	createArticle(author: $author, title: $title, markdown: $markdown, description: $description) {
        ...DefaultArticle
    }
}
